%section#images
  %article.image
    #drop_box
      Drop your image here
    #status
:javascript
  $("#images").dndUploader({
    url : "/upload"
  })

  function updateImage(image) {
    $("#drop_box").append("<img src='"+image.image+"' />");
  }
